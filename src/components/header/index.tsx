import { StyledHeader } from '../../styles/components/header/header';
import Buttons from './buttons';

export default function Header() {
  return (
    <StyledHeader>
      <h1>Lettrex</h1>
      <Buttons/>
    </StyledHeader>
  );
}
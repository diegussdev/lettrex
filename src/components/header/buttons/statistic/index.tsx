import { StyledButton } from '../../../../styles/components/header/buttons/button';

import StatisticIcon from '../../../../assets/chart.svg';

export default function StatisticButton() {
  return (
    <StyledButton>
      <StatisticIcon />
    </StyledButton>
  );
}